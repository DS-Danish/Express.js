// AI-GEN START - ChatGPT GPT-4
doctype html
html
head
    title Create Folder
body
    h1 Create a Folder in S3
    form(action="/S3Post_user_pic/:username" method="POST" enctype="multipart/form-data")
        label(for="folderName") Folder Name:
        input(type="text" id="folderName" name="folderName" required)
        input(type="file" name="profilePic" required)
        button(type="submit") Create Folder
// AI-GEN END
