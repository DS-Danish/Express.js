html
   head
      title Login
   body
      h4 #{message}
      form(action = "/loginUser" method = "POST")
         input(name = "name" type = "text" required placeholder = "User ID")
         input(name = "password" type = "password" required placeholder = "Password")
         button(type = "Submit") Log in